---
import BaseLayout from "../../layouts/BaseLayout.astro";
import WorkCard from "../../components/WorkCard.astro";
import { projects } from "../../data/projects";

const base = import.meta.env.BASE_URL;
---

<BaseLayout
  title="Work — Bartek Dzięgiel"
  description="Selected case studies in product design, UX, and branding."
>
  <section class="work-hero">
    <h1 class="work-hero__title">Work</h1>
  </section>

  <section class="work-grid">
    {projects.map((project) => (
      <WorkCard
        href={`${base}/work/${project.slug}`}
        slug={project.slug}
        image={project.image}
        video={project.video}
        alt={project.alt}
        title={project.title}
        subtitle={project.subtitle}
        year={project.year}
      />
    ))}
  </section>
</BaseLayout>

<style>
  .work-hero {
    max-width: var(--content-max-width);
    margin-inline: auto;
    padding: 120px var(--space-3) var(--space-5);
  }

  .work-hero__title {
    font-family: var(--text-display-font);
    font-weight: var(--text-display-weight);
    font-size: var(--text-display-size);
    letter-spacing: var(--text-display-letter-spacing);
    line-height: var(--text-display-line-height);
    color: var(--color-fg);
  }

  .work-grid {
    max-width: var(--content-max-width);
    margin-inline: auto;
    padding: 0 var(--space-3) var(--space-6);
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-3);
  }

  @media (max-width: 64rem) {
    .work-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 40rem) {
    .work-hero__title {
      font-size: 2.5rem;
    }

    .work-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
